#include <bits/stdc++.h>
using namespace std;

int main() {
vector<string> temp;
vector<vector<string>> grid;

for(int i = 0; i < 10; i++) {
    string a, b, c;
    cin >> a;
    temp.push_back(a);
    temp.push_back(b);
    temp.push_back(c);
    grid.push_back(temp);
}

for(int i = 0; i < 4; i++) {
    for(int j = 0; j < 4; j++) {
        if(grid[i][j] == "x") {

        }
    }
}

}

